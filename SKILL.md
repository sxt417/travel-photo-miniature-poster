---
name: travel-photo-miniature-poster
description: 将上传的旅游照片进行场景理解和空间重建，按真实高度关系和统一比例尺生成 3:4 竖版、带全球建筑类型自适应地下剖面、右上角场景平面图及底部自动填充地标信息表的实体建筑模型摄影质感等距微缩沙盘海报；强制独立 3D 几何、基于用途年代结构与地理环境的可信地下世界、可核验地点信息、与沙盘一致的场地平面关系、受控地形压缩与微小接触阴影，拒绝照片投射、航拍贴图、高度图式地形、错误文字和幻想化地下空间。用于“旅游照片转 3D 沙盘海报”、“微缩景观纪念海报”、“等距视角地标模型”或“把景点照做成微缩世界”等需求。
---

# 旅游照片 3D 微缩沙盘海报

将上传照片中的地点重新设计成一件真实存在的实体建筑微缩模型，再对该模型进行摄影棚拍摄。不要生成“缩小的真实世界”，而要生成“模拟真实世界的实体建筑模型”。

## 工作流程

1. 确认至少有一张可用的原始照片。如果没有附件或无法访问图片，请用户上传后停止。
2. 分析场景身份、核心地标、建筑语言、材质、植被、地貌、道路、水体和空间关系；同时读取 [全球建筑通用地下剖面生成规则](references/global-underground-sections.md)，判断建筑类型、年代、用途、材料、结构体系、地形与水文，建立“已知事实 / 类型推断 / 保守回退”三级地下证据档案。
3. 建立“真实尺度与比例契约”：记录主要建筑、地标、树木、船只、地形、基础和地下空间的真实高度、深度或可靠估算范围，确定统一模型比例尺；如大型自然地貌必须压缩，记录唯一的地形垂直压缩系数。
4. 确定两行英文标题：
   - 能够可靠识别时，上方小字使用英文国家名称，下方大标题使用英文地点或地标名称。
   - 无法确定时，上方小字使用 `TRAVEL SOUVENIR`，下方大标题使用 `TRAVEL MEMORY`、`SUMMER GETAWAY` 或 `MINIATURE ESCAPE`。
   - 不得根据薄弱线索虚构精确地点。除非用户要求，不要为了强行识别地点而上网搜索。
5. 自动建立底部信息表数据：`Name`、`LOCATION`、`BUILT`、`HEIGHT`、`COORDINATES`。能可靠识别地点时，优先用官方或权威资料核对；字段必须与国家名、主标题和沙盘身份一致。不能核实时使用本 Skill 规定的诚实回退值，不得沿用参考图示例或编造精确数据。
6. 从重建后的场景空间关系提取无文字场景平面图，使建筑、道路、花园、水体、围墙与下方沙盘保持一致。
7. 按本 Skill 的优先级组装提示词，将尺度契约、地下证据档案、建筑类型匹配结果、场景平面图结构、底部信息表和上传照片一并作为约束，使用可用的图像生成工具直接生成。
8. 按“场景平面图质量门禁”“信息与文字质量门禁”“地下剖面质量门禁”“比例与编辑质量门禁”“3D 建模质量门禁”和“海报质量门禁”检查结果。任一硬性条件失败时，针对失败项重新生成一次。
9. 返回生成图像并简要说明。除非用户要求，不要展示完整内部提示词。

## 生成优先级

严格按以下顺序取舍：

1. 实体沙盘模型语言
2. 独立 3D 几何结构
3. 真实尺度关系与高度比例
4. 地下结构与建筑类型、年代、用途及地理环境的可信关联
5. 地点识别度
6. 地标信息的准确性与文字可读性
7. 场景平面关系与沙盘一致性
8. 原图内容细节忠实度
9. 海报排版

物理模型语言的优先级高于“看起来像真实景观”。

## 核心视觉定义：实体建筑微缩模型，而非地形贴图

最终图像必须首先被看作“一件真实存在、被摄影棚拍摄的实体建筑沙盘模型”，其次才是对真实地点的还原。

目标不是从高空俯拍真实地点，也不是将照片投射到 3D 地形上。必须表现为：

> photorealistic studio photograph of a handcrafted architectural scale-model diorama, with fully modeled miniature geometry, physical depth, tiny contact shadows, visible object separation, and realistic scale-model construction.

整体第一眼必须读取为 `a miniature place on a square cuboid architectural model base`，而不是 `a miniature mountain sitting on a platform`。

### Geometry-first 原则

所有主要视觉元素必须由具有真实厚度的独立 3D 几何体构成。必须独立建模：

- 建筑墙体与屋顶；
- 窗框、屋檐与阳台；
- 道路与人行道；
- 路缘石与挡土墙；
- 台阶与桥梁；
- 树木与灌木；
- 岩石与岸线；
- 船只、车辆与人物；
- 水体边缘与地形层级；
- 地下空间、建筑基础、地基构件、土层、岩层、根系与地下水边界。

树林不得表现为连续的绿色纹理或模糊植被贴图。近景和中景树木必须能辨认出独立树冠、树干与树木之间的空隙，并产生微小接触阴影。

建筑不得表现为平面航拍纹理。必须清楚看到墙体高度、屋顶厚度、立面转折以及建筑之间的遮挡关系。

禁止：

- `flat photographic texture`
- `projected photo texture`
- `satellite-image appearance`
- `aerial-photo texture`
- `texture-mapped terrain`
- `heightmap-only terrain`
- `flat printed map appearance`
- `vegetation carpet`
- `baked lighting`

## 真实尺度与高度比例

默认先遵循真实世界的高度关系，再考虑海报适配。不得仅凭“看起来协调”随意放大地标、压低山体或统一建筑高度。

生成前建立内部尺度表，至少记录：

| 对象 | 真实高度或估算范围 | 相对高度 | 模型高度 | 置信度 | 是否锁定 |
|---|---:|---:|---:|---|---|
| 核心地标 | 米 | 相对基准物 | 模型单位 | 高/中/低 | 是 |
| 主要建筑 | 米或楼层推算 | 相对核心地标 | 模型单位 | 高/中/低 | 是 |
| 典型树木 | 按树种或当地常见范围 | 相对建筑 | 模型单位 | 中/低 | 是 |
| 船只、车辆、人物 | 常见真实尺寸 | 相对建筑 | 模型单位 | 中 | 是 |
| 局部与区域地形 | 海拔差或可靠估算 | 相对底座边长 | 模型单位 | 高/中/低 | 是 |
| 地下空间与基础 | 埋深、净高、基础深度或估算范围 | 相对地表与建筑 | 模型单位 | 高/中/低 | 是 |

尺度规则：

- 以方形底座在现实中代表的单边距离为 `R`，模型底座单边长度为 `B`，水平比例尺为 `S = B / R`。
- 建筑、构筑物、道路、桥梁、船只、车辆、人物、树木、局部地形、基础和地下空间默认使用相同的水平与垂直比例尺：`H_model = H_real × S`，地下深度使用 `D_model = D_real × S`。
- 主要建筑之间、建筑与地标之间必须保留真实高度比。例如真实高度为 30 m 与 15 m 的两座建筑，模型高度仍应约为 2:1，不得为了构图改成近似等高。
- 楼层可辨但缺少精确尺寸时，可按建筑类型估算层高并使用范围，不得虚构带小数点的精确高度。无法可靠判断时使用相对比例和低置信度标记。
- 树种不明时按当地常见成熟树高范围估算；树林允许自然个体差异，但其总体尺度必须与建筑、鸟居、船只和人物相容。
- 已可靠识别且精确高度会显著影响结果的地标，可查找官方或权威来源核实尺寸；不得为了获得尺寸而根据薄弱线索强行识别地点，也不得把不确定记忆写成精确事实。
- 在生成提示词中同时写出关键对象的真实高度、相对高度比和模型高度关系；不要只写“realistic proportions”。

## 大型自然地形的受控压缩

山脉、峡谷、丘陵、岛屿和海岸首先尝试使用真实比例。只有真实高差会导致底座无法完整入镜、建筑与水岸完全不可读或作品退化为单一山体雕塑时，才允许使用 `architectural model vertical compression`。

- 设未压缩的最高地形模型高度为 `H_raw`，允许的地形高度为 `H_cap`，统一压缩系数为 `C = min(1, H_cap / H_raw)`。
- 所有同一连续地貌中的自然地形统一使用 `H_terrain = H_real × S × C`。不得给每座山、每段山脊或每个坡面使用不同压缩系数。
- 压缩只作用于大型自然地形的垂直高度，不改变建筑、构筑物、树木、船只、车辆或人物的比例。
- 压缩后仍须保留各山体、山脊、峡谷和坡面的真实高低顺序及相对高度比。
- 核心地标山体的 `H_cap` 默认取 `0.22B–0.25B`；除非用户要求或真实场景本身极低缓，不得低于 `0.20B`。普通背景地形可取 `0.12B–0.20B`。
- 选择一个明确目标值和允许误差，不得只把宽泛范围交给图像模型。例如使用 `H_cap = 0.23B，容差 ±0.02B`，而不是只写“20%–25%”。
- 高度从山脚所在模型顶面量到最高点，不得从画布底边、底座侧面或树林顶部开始计算。
- 若使用了 `C < 1`，必须在内部尺度表中记录；后续编辑沿用同一 `C`，除非用户明确要求改变地形高度。
- 山体必须表现为层层雕塑出来的微缩地貌，而不是一个巨大连续锥体；山体、森林、湖泊、道路、建筑和地标共同构成完整微缩世界。

## 局部修改与比例锁定

编辑已生成海报时，先区分用户要求改变的变量与必须保持的变量。透视角度、物理高度、占地宽度、位置、材质、光照、模型整体大小和排版是相互独立的变量。

- 用户只要求修改其中一个变量时，其他变量全部锁定；不得借局部修改重新设计整块沙盘。
- 每次编辑沿用上一版本的尺度表、统一比例尺 `S`、地形压缩系数 `C` 和所有未点名对象的模型高度。
- 用户只要求改变透视角度时，只调整顶面可见量、远近坡缩短关系、消失方向、遮挡和投影；保持对象的物理高度、占地、位置及相对比例不变。
- 透视修改不得重新应用默认地形压缩比例，不得在同一次修改中额外升高、压低、放大或缩小山体。
- 对图像生成工具明确写出：`change projection only; preserve the scale contract, physical height, footprint, bounding box and relative proportions`。
- 在没有真实 3D 数据的图像编辑中，未点名对象的画面包围盒宽高变化原则上不得超过 5%–8%；超出即判定为意外比例漂移并重新生成。
- 如果用户明确要求修改高度，则只更新尺度表中的对应高度字段，并保持透视、占地与其他对象比例不变；必要时重新计算并明确记录 `C`。

## 原图使用原则

上传照片用于确定场景身份、核心地标、建筑语言、材质、植被、地貌与空间特征。

保持 `semantic fidelity`，而不是 `camera-view fidelity`。忠于照片告诉了什么，不忠于照片原本如何取景。不得直接复制原照片的透视构图。必须将真实场景重新解释为完整的俯视建筑沙盘：

```text
PHOTO
↓
scene understanding
↓
spatial reconstruction
↓
miniature architectural model
```

不得使用：

```text
PHOTO
↓
depth extrusion
↓
texture projection
```

## 原图内容忠实度

- 只重建原图明确呈现或能够直接推断的场景。
- 将关键地标或环境主体保留为识别核心，但不必保留原相机位置、透视或景物在画面中的大小。
- 只扩展必要的近邻环境，使其形成完整、可信的方形沙盘；不要把每个场景都扩展为整座城市地图。
- 保留地域特征、当地建筑语言、材质、平面关系、植被和水岸形态。
- 不得添加无关著名地标或原图未支持的奇幻建筑。
- 人物、车辆和船只只作为微型尺度参照，不得成为全尺寸主体。

## 地标信息核验与自动填充

底部信息表不是装饰性占位符。生成前必须根据已经可靠识别的地标自动填写以下五项，并保持英文名称、大小写、数字、单位和标点清晰可读：

| 标签 | 自动填写内容 | 格式 |
|---|---|---|
| `Name` | 地标最常用的英文正式名称；与主标题指向同一对象，但保留正常英文标题大小写 | 例如 `Christ the Redeemer` |
| `LOCATION` | 城市或行政区，加英文国家名 | `City, Country` |
| `BUILT` | 官方完工、落成或启用年份；确有建造区间时可写年份范围 | `1931` 或 `1887–1889` |
| `HEIGHT` | 地标本体官方高度；自然地标则使用最具识别意义且可核验的海拔、落差或高度 | 数值与单位间留空格，例如 `38 m` |
| `COORDINATES` | 地标中心点或主入口的纬度、经度 | 十进制度保留 4 位小数并加半球字母，例如 `22.9519° S, 43.2105° W` |

自动填充规则：

- 一旦地点可被可靠识别，为这些字段核验官方机构、遗产名录、政府、博物馆或其他权威来源；不同来源冲突时优先采用地标管理方或官方资料。
- `Name`、上方国家名、主标题、`LOCATION` 和 `COORDINATES` 必须描述同一地点。参考图中的文字只用于样式和版式，不得复制其中的示例值。
- 主标题固定使用全大写；信息表中的 `Name` 和 `LOCATION` 值使用正常英文标题大小写，国家名不强制全大写。
- `BUILT` 采用最能代表地标完成的年份，不把奠基、开工、翻修或列入遗产名录的年份误写为建成年份。
- `HEIGHT` 默认不含基座时应明确采用官方口径；若官方常用总高包含基座，则沿用该口径。不要混用海拔、建筑高与雕像高。
- 自然景观没有建造年份时，`BUILT` 填 `N/A`；无法确认可靠高度时，`HEIGHT` 填 `N/A`。无法可靠识别精确地点时，`LOCATION` 填 `Location unknown`，`COORDINATES` 填 `N/A`，不得伪造坐标。
- 若用户明确提供任一字段，优先保留用户值；只有发现明显矛盾时才提示并使用可核验值。
- 在内部记录字段值、来源类型和置信度；除非用户要求，不在成图中显示来源、脚注、网址或置信度。

## 场景平面图

默认在海报右上方加入一张小型无文字场景平面图。它是下方沙盘的正投影空间摘要，用于快速说明核心地标、道路、花园、水体、桥梁、围墙和地形边界之间的平面关系；不是拍摄位置图、导航地图或真实地图截图。

平面图规则：

- 默认北向上；若资料无法可靠确定北向，则使平面图与沙盘的主轴方向一致，不添加虚假的北向信息。
- 平面图必须从已经确定的沙盘空间重建结果中提取，与下方沙盘使用同一组建筑占地、道路走向、水岸曲线、花园分区、桥梁位置和围墙边界；不得为了图案对称而改变真实平面关系。
- 只保留有助于理解场景的一级与二级空间结构。核心地标使用较深暖灰色块，次要建筑使用浅暖灰色块，道路与围墙使用深炭灰细线，水体使用低饱和灰蓝色；植被只用少量克制的树冠点或绿地色块表示。
- 不显示拍摄位置、人物站位、图钉、误差圈、视线锥、路线、起终点或导航状态。
- 不使用任何文字、数字、坐标、地名、比例尺文字、指南针字母、图例、地图服务商标或 emoji。若北向可靠，可使用纯几何无文字箭头。
- 不得出现卫星图、航拍照片纹理、真实地图截图、路线导航 UI、等高线噪声或版权水印。
- 平面图保持干净的建筑总平面／场地总平面线稿质感，背景与海报暖象牙底色融合；不得做成独立卡片、手机地图界面或带厚重边框的信息框。
- 平面图不得遮挡国家名、主标题、沙盘或阴影，也不得为了容纳平面图改变标题与沙盘的锁定位置。

## 方形建筑沙盘结构

- 沙盘底座必须是规则四边形立方体／方形棱柱（`square cuboid architectural model base`）：顶面为完整正方形，四条外边笔直，四个角均为明确的 90° 直角，四个侧面垂直且底座厚度一致。
- 透视投影可以使正方形顶面在画面中呈菱形，但不得改变底座的真实几何；禁止生成梯形、六边形、圆形、椭圆形、圆角、倒角、缺角、外扩、收腰或随地形起伏的不规则底座。
- 地下剖面、观察窗和移除土体只能发生在规则立方体的内部，不得改变四边形底座的外轮廓，不得让剖面形成突出的舱体、凹凸边界或额外台座。
- 在精准切割、具有足够地下剖面深度的分层方形底座上搭建模型，并将顶面从边缘到边缘填满具有实体层级的场景；底座不得退化为只有一条薄木边。
- 旋转底座，使其在透视中呈菱形，前端尖角指向画面下方。
- 使用约 45–50° 向下俯视的高位等距视角。
- 显示笔直的垂直切面；前侧与一个相邻侧面以地下剖面取代单纯的深色装饰边，另两侧可保留窄条深色木材或模型框架。
- 缩小沙盘占比，确保底座四条边、四个角以及所有高处结构完整入镜，不得被画布边缘截断。
- 沙盘整体尺寸和画布安全边距遵循“锁定海报版式”；沙盘任何部分及其阴影都不得贴边。

## 地下剖面场景

默认生成地上与地下同时可读的双层沙盘。地下部分不是装饰纹理，而是从方形底座前侧及一个相邻侧面切开的实体剖面世界，用于解释建筑为什么存在、如何被支撑、如何被使用，以及其结构与历史如何沉积；地表顶面保持完整连续，不使用透明地面覆盖整块沙盘。

### 地下剖面高度控制

地下剖面必须清晰，但底座应保持低矮、克制，不得形成比地表主体更醒目的厚重土方块。设方形底座真实模型边长为 `B`，从地表顶面到底座底面的垂直厚度为 `T_base`：

- 默认使用 `T_base = 0.12B–0.16B`，目标值为 `0.14B`，允许误差为 `±0.02B`。
- 天坛、祭坛、寺庙台基、古典园林建筑及其他只有浅基础、夯土台基或浅层排水的场景，使用更低的 `T_base = 0.10B–0.13B`，目标值为 `0.12B`；不得为了增加戏剧性把普通坛基拉成深层地下建筑。
- 只有存在可核验的深基础、墓室、地宫、地下设备层、深桩、山体洞窟或显著地下水关系时，才可提高厚度；通常不得超过 `0.18B`，并在内部尺度表中记录原因。
- 在最终画面投影中，地下剖面可见高度默认占完整沙盘组画面高度的 `22%–28%`，目标为 `25%`；浅基础／坛基类控制在 `18%–22%`，目标为 `20%`；有可靠深层结构时也不得无依据超过 `30%`。
- 高度从沙盘地表顶面量到底座最低点，不包含建筑、树木、栏杆或底座投影。不得把透视造成的前侧面放大误当成真实地下深度。
- 地下内容继续沿用统一比例尺 `S`，不得为满足高度目标压扁、拉长或放大真实结构。若真实深度超过版式上限，优先减少无信息的深层土体、选择更有效的剖切位置或使用局部观察窗，不压缩关键基础、墓室和工程构件。
- 禁止用大面积无信息土层、重复岩层、空洞柱廊或虚构地下大厅填高底座。地下信息应集中在靠近地表的必要范围内，使地表建筑保持明确主导地位。

每次生成前必须读取并执行 [全球建筑通用地下剖面生成规则](references/global-underground-sections.md)。先判断建筑属于陵墓、宗教、宫殿防御、塔与纪念碑、桥梁交通、山体建筑、遗址、现代建筑或临水建筑中的一种或多种，再从结构、功能、工程、地质、历史、场景六类元素中选择最相关的 `2–5` 类组合；不得把六类元素全部堆满。

### 地下证据等级

1. **A级：已知事实。** 有官方资料、考古报告、工程图纸、遗产档案或可靠历史记录时，优先还原真实地下室、墓室、通道、排水、桥墩、桩基、地下河、洞穴或考古层，并记录来源、真实埋深、净高与置信度。
2. **B级：类型推断。** 缺少该地标的精确地下资料，但建筑类型、年代、用途、材料、结构体系、地形或水文足以支持工程上或历史上合理的推断时，可生成“最可能存在”的地下结构。推断内容必须能解释承重、排水、防潮、祭祀、埋葬、储藏、防御、运输或维护中的至少一项；不得把推断伪装成已证实事实。
3. **C级：保守回退。** 无法形成可靠的功能性推断时，只表现与建筑类型相符的基础、承台、墙基、桩基或锚固，以及土层、岩层、根系、地下水和必要排水。不得为了丰富画面添加墓穴、密室或通道。

### 地下剖面规则

- 地下空间、基础和埋深沿用地表同一比例尺 `S`，不得为了“更好看”单独放大洞穴、墓室、地铁、基础或工程系统。
- B级推断使用保守的常见尺寸范围与低或中置信度，不生成带小数点的伪精确埋深；后续编辑必须沿用同一推断类型与尺寸契约。
- 墓穴不属于所有宗教建筑的默认配置；密道不属于所有宫殿或城堡的默认配置；地铁连接不属于所有现代公共建筑的默认配置。只有建筑类型、年代、用途和场地共同支持时才可加入。
- 若真实地下深度过浅而难以读取，优先使用局部剖面观察窗、移除遮挡土体或增强材质与边缘对比；不得改变结构尺寸。只有用户明确要求示意性夸张时，才可记录并使用独立的地下垂直展示系数。
- 剖切线必须服务于核心地标，尽量穿过或靠近主要建筑基础、功能空间或关键水文结构，同时避免切断地表主体的识别轮廓。
- 地层必须至少区分表土、夯土或填土、承压土或砂石、基岩或水层中实际相关的层级；土层、岩层、河床和地下水必须具有连续厚度与可信接触关系。
- 基础、管线、锚固、地下空间、根系与土体之间必须有清晰边缘、遮挡和微小接触阴影。剖面应像博物馆建筑模型或考古展示模型，而不是印在底座侧面的插图。
- 可加入极少量石棺、器物、检修梯、考古脚手架或微型人物增强尺度与叙事，但只能出现在对应证据等级允许的空间中，不得制造冒险、恐怖或奇幻氛围。
- 地下层计入完整沙盘组的整体包围盒，必须与地上模型一起满足 3:4 海报中锁定的沙盘范围；不得向下进入底部信息表或突破安全间距，也不得为了容纳地下层压低地上建筑。
- 地上与地下使用同一等距相机、光照方向和模型材质语言。剖面内部可略微提亮以保证可读性，但不得像发光橱窗或独立插图；地下内容不得喧宾夺主。

### 地下总控指令

提示词中必须加入：

> For every landmark or building, automatically generate a historically or structurally plausible underground sectional scene based on the building’s type, era, function, material, and terrain. The subterranean part may include foundations, burial chambers, corridors, drainage systems, archaeological layers, bedrock, groundwater, or hidden service spaces, but must remain realistic, architecture-related, and globally applicable to landmarks around the world.

## 微缩模型立体光影

使用柔和的三分之四摄影棚主光，而不是完全均匀的正上方照明。整体仍然明亮柔和，但必须保留：

- 建筑底部的微弱接触阴影；
- 屋檐与立面的细小阴影；
- 树冠内部和树木之间的遮挡阴影；
- 桥梁、车辆和人物下方的微小阴影；
- 河岸、道路和地形层级之间的环境遮蔽（ambient occlusion）；
- 模型与底座之间清晰但柔和的阴影关系。

阴影不得戏剧化，但必须足以帮助观察者感知每个微缩物体的真实厚度。

禁止：`flat overhead lighting`、`shadowless rendering`、`evenly lit texture-map appearance`。

## 材质、对焦与背景

- 表现高级手工建筑模型材质：精细石膏或矿物粉地形、独立植被、树脂水体、喷涂木件、石材、混凝土、沥青、陶土、玻璃和金属。
- 保持整块模型清晰对焦，用实体厚度、层级、遮挡和阴影表现立体感，不得依赖景深模糊营造“微缩”。
- 使用自然、克制的饱和度。避免塑料玩具感、过度锐化、程序化重复、光滑无瑕疏的 CGI 表面。
- 将底座放在干净、哑光的暖象牙米白色无缝摄影棚背景上，参考色约为 `#F5F2EA`。只允许由摄影棚光线造成极轻微的自然明度过渡；不要出现蓝色偏色、天空感、地平线、云朵、纸张纹理、道具、画框或装饰边框。

## 海报布局与字体排印

视觉版式基准见 [assets/layout-reference-3x4.png](assets/layout-reference-3x4.png)。该图只用于复刻 3:4 构图、标题层级、右上角平面图和底部信息表的样式；其中地标名称、城市、年份、高度和坐标均为示例占位，不得复制为成图内容。

### 锁定海报版式

版式以 `750 × 1000 px` 为参考画布，其他 3:4 竖版分辨率必须按百分比等比换算，不得按固定像素机械套用。最终输出比例固定为 `3:4`，即 `W:H = 3:4`；推荐输出 `1536 × 2048 px` 或其他严格 3:4 分辨率。

| 元素 | 750 × 1000 参考范围 | 归一化范围 |
|---|---:|---:|
| 上方国家名 | `x ≈ 320–430 px`，`y ≈ 88–104 px` | 居中，`8.8%–10.4%H` |
| 下方主标题 | `x ≈ 195–560 px`，`y ≈ 119–184 px` | 居中，最大宽 `50%–54%W`，`11.9%–18.4%H` |
| 场景平面图 | `x ≈ 540–720 px`，`y ≈ 22–222 px` | `72%–96%W`，`2.2%–22.2%H` |
| 完整沙盘组（含高处结构、地表与地下剖面，不含大范围投影） | `x ≈ 15–735 px`，`y ≈ 220–710 px` | `2%–98%W`，`22%–71%H` |
| 底部信息表 | `x ≈ 50–700 px`，`y ≈ 750–950 px` | `6.7%–93.3%W`，`75%–95%H` |
| 底部留白 | 约 `50 px` | 约 `5%H` |

版式硬约束：

- 沙盘组水平居中，中心线固定在 `50%W` 附近。
- 沙盘组目标宽度为 `94%–96%W`，目标高度为 `47%–50%H`，两者允许误差均为 ±2%；顶部约从 `22%H` 开始，底座最低点及阴影不得低于 `71%–72%H`。这是默认且唯一的模型画面占比。
- 沙盘最左与最右分别约位于 `2%–3%W` 和 `97%–98%W`；外轮廓必须完整入镜并保留最少 `2%W` 的安全边距。
- 地标最高点不得遮挡国家名或主标题；标题区与沙盘区应有清晰层级，允许阴影或高处构件进入文字下方负空间，但不得穿过字形。
- 沙盘、地下剖面、底座、树木、山体、建筑和投影均须完整入镜；四条边、四个角和前端尖角清晰可见。
- 场景平面图固定在右上方 `72%–96%W`、`2.2%–22.2%H` 的留白区，使用纤细暖灰线稿；不得移入信息表或遮挡标题与沙盘。
- 国家名和主标题位于上方中央；左上方保持纯净留白。除右上方场景平面图外，不放置其他图形、引题或装饰。
- 沙盘与信息表之间保留约 `3%–5%H` 的呼吸空间；沙盘投影不得进入信息表边框。

### 两行英文排印

- 文字顺序固定为：上方国家名，下方地点或地标主标题。
- 国家名使用深炭灰或近黑色、常规或中等字重的窄体无衬线大写字母，水平居中；在 `750 × 1000` 参考画布上，字高约 `14–16 px`，字距约 `0.10–0.14em`，整体位于 `8.8%–10.4%H`。优先使用 `DIN Condensed`、`Roboto Condensed` 或等价窄体无衬线。
- 主标题使用深炭灰色、极粗窄黑无衬线大写字母，水平居中；在 `750 × 1000` 参考画布上，字高约 `58–64 px`、紧凑行高约 `0.95–1.0`。优先使用 `Bebas Neue`、`League Gothic`、`Anton` 或等价超窄粗体。
- 主标题必须保持单行，目标宽度约 `48%–54%W`，最大文字宽度为 `70%W`。名称较长时优先收窄字面或等比减小字号，不得换行、挤出画布或覆盖沙盘主体。
- 国家名与主标题之间保留约 `1.2%–1.8%H` 的垂直间隔。
- 例如上方小字使用 `CHINA`，下方大标题使用 `TEMPLE OF HEAVEN`。
- 标题区只使用这两行简短英文。场景平面图保持无文字；除底部信息表规定的标签与值外，不得添加引题、段落、标志、徽章、水印或虚构信息。

### 底部信息表：一比一复刻参考设计

底部固定加入一个无填充、单色细线的两行信息表。它与暖象牙背景融为一体，不是悬浮卡片，不使用圆角、阴影、图标、色块或装饰。

- 外框位于 `x = 6.7%–93.3%W`、`y = 75%–95%H`，宽约 `86.6%W`、高约 `20%H`；在 `750 × 1000` 参考画布上约为 `650 × 200 px`。
- 边框与分隔线使用 `1 px` 深炭灰线，参考色 `#333333`；其他分辨率按比例缩放，但视觉上线宽保持克制。
- 水平中线固定在 `85%H`，将表格分为两个等高行。
- 上排分为两个等宽单元：`Name` 占 `6.7%–50%W`，`LOCATION` 占 `50%–93.3%W`。
- 下排分为三列：`BUILT` 占 `6.7%–28.4%W`，`HEIGHT` 占 `28.4%–50%W`，`COORDINATES` 占 `50%–93.3%W`。因此中轴竖线贯穿全表，左下半区再等分为两列。
- 每个单元格左内边距约 `2.6%W`（750 px 画布约 `20 px`）；标签距所在行顶边约 `2.2%–2.8%H`，值位于标签下方约 `1.0%–1.4%H`。
- 所有文字左对齐、垂直节奏一致。标签使用深炭灰等宽字体，推荐 `IBM Plex Mono`、`Roboto Mono` 或等价字体；`Name` 保留参考图的首字母大写，其余标签固定全大写。参考字号约 `13–15 px`，常规字重。
- 字段值使用同一等宽字体，参考字号约 `22–24 px`，中等字重，行高紧凑；`COORDINATES` 可缩至 `20–22 px` 以保持单行。值不得换行、溢出、挤压边框或被分隔线截断。
- 表格文字统一使用近黑色 `#171717`，不使用彩色强调。数字使用等宽字形；度数符号必须正确，纬度在前、经度在后。
- 字段内容由“地标信息核验与自动填充”自动生成。不得照抄参考图的示例数据，不得出现标题为一个地标而 `Name`、城市、年份、高度或坐标属于另一个地标的情况。

## 提示词组装

将分析结果组装为清晰的生成规格，依次写出：

1. 原图提供的场景身份、必须保留的核心地标与空间关系；
2. 已核验并准备自动填写的 `Name`、`LOCATION`、`BUILT`、`HEIGHT`、`COORDINATES` 五项值，以及它们与标题、地点身份的一致性；
3. 右上方场景平面图的一级、二级空间结构及其与沙盘的逐项对应关系；
4. “实体建筑沙盘模型摄影”的核心视觉定义；
5. 真实高度表、统一比例尺、关键对象相对高度比及置信度；
6. Geometry-first 的独立建模对象与必须看见的厚度、边缘、层级和遮挡；同时写明建筑类型、地下证据等级、从六类构成中选择的 `2–5` 类元素、每项内容与地表建筑的功能关系、剖切位置、真实或保守估算深度，并明确本次 `T_base/B` 目标值及地下剖面占完整沙盘组的画面高度比例；浅基础／坛基类默认写入 `T_base = 0.12B`、画面高度约 `20%`，加入地下总控指令；
7. 大型自然地形是否压缩；如压缩，写出唯一的 `C`、明确的 `H_cap` 和允许误差；
8. 材质、独立树木、微缩物件与微小接触阴影；
9. 明确输出为 `3:4 portrait poster, W:H = 3:4`，并写出完整地上与地下双层规则四边形立方体底座（正方形顶面、四条直边、四个 90° 角、垂直侧面、统一厚度）、暖象牙背景、两行准确英文、右上方无文字场景平面图及锁定版式：国家名位于 `8.8%–10.4%H`，主标题位于 `11.9%–18.4%H`，平面图位于 `72%–96%W` 与 `2.2%–22.2%H`，完整沙盘位于 `2%–98%W` 与 `22%–71%H`，底部信息表位于 `6.7%–93.3%W` 与 `75%–95%H`；
10. 明确底部信息表为无填充、无圆角、无阴影的 `1 px` 深炭灰网格；上排两等分，下排左半再等分，标签和值全部使用等宽字体并严格单行；
11. 将全部贴图化、航拍化、高度图化、虚构平面关系、错误文字、乱码、字段不一致、地下证据等级错配、随机洞穴、幻想地下空间、任意比例漂移与无阴影渲染禁止项统一放在末尾。

## 场景平面图质量门禁

出现以下任意情况必须判定失败并修正场景平面图：

- 平面图中的核心地标占地、道路走向、水岸、水池、桥梁、花园分区或围墙位置与下方沙盘明显不一致；
- 平面图为了视觉对称而新增、删除、移动或镜像真实场景结构；
- 平面图出现拍摄点、图钉、人物站位、误差圈、视线锥、导航路线或起终点标记；
- 平面图包含卫星图纹理、真实地图截图、导航界面、地图服务商标、文字、数字、坐标、图例或版权水印；
- 平面图过度复杂而无法快速辨认一级空间结构，或过度简化到无法对应核心地标与主要道路、水体；
- 平面图与海报标题、沙盘或阴影重叠，或使用厚重卡片边框破坏整体留白。

合格结果必须满足：场景平面图是下方沙盘的无文字正投影摘要，核心地标、主要路径、水体、花园、桥梁和边界可相互对应，且不存在任何摄影点位或导航信息。

## 信息与文字质量门禁

出现以下任意情况必须判定失败并针对文字或数据重新生成：

- `Name`、国家名、主标题、`LOCATION` 或 `COORDINATES` 指向不同地点；
- 沿用了参考图中的 `Eiffel Tower` 等示例占位值，或字段内容与上传照片无关；
- `BUILT`、`HEIGHT` 或 `COORDINATES` 是未经核验的虚构精确值；
- 自然景观或无法核实的字段没有使用规定的 `N/A` 回退，而是凭空补全；
- 标签拼写、大小写、数字、单位、度数符号、半球字母、逗号或小数位错误；
- 任一标签或值出现乱码、重影、断字、变形、换行、裁切、溢出或被网格线穿过；
- 表格列宽、行高、分隔线或文字层级明显偏离锁定结构；
- 信息表出现圆角、填充色块、阴影、图标、彩色强调或额外说明文字。

合格结果必须满足：五个字段完整、单行、可读、与地标身份一致；信息有据可循；无法核实的内容诚实回退；表格在视觉上与参考设计的两行网格、等宽字体和细线风格一致。

## 地下剖面质量门禁

出现以下任意情况必须判定失败并针对地下内容重新生成：

- 没有读取全球通用规则、没有判断建筑类型，或地下内容与建筑用途、年代、材料、结构体系、地形及水文明显无关；
- 已有可靠地下资料却被无依据的类型推断替代，或A级事实、B级推断与C级回退在内部证据档案中混淆；
- B级推断不能解释承重、排水、防潮、祭祀、埋葬、储藏、防御、运输或维护中的任何一项；
- 未经类型与证据支持就加入墓穴、地宫、密室、军事隧道、地铁连接或大型地下大厅；
- 六类地下构成全部堆满，信息过载，或没有从中选择最相关的 `2–5` 类；
- 缺少结构层和地质层，导致地下空间悬浮、基础不承重、水下构件无承压层或山体锚固无岩体连接；
- 地下内容只是无信息土块、平面纹理或独立插图，无法读出层理、厚度、剖切、遮挡和结构关系；
- 土层、岩层、河床、地下水、根系、基础和功能空间的接触关系不连续或违反基本工程逻辑；
- 地下出现随机洞穴、宝藏、熔岩、幻想遗迹、科幻或超自然机关，或采用恐怖冒险氛围；
- 地下空间在面积、亮度、复杂度或叙事强度上压过地表建筑主体；
- 未记录深层结构依据时，`T_base` 超出默认 `0.12B–0.16B`，或地下剖面可见高度超过完整沙盘组画面高度的 `28%`；
- 天坛、祭坛、寺庙台基及其他浅基础场景的 `T_base` 超过 `0.13B`，或地下剖面可见高度超过完整沙盘组画面高度的 `22%`；
- 为增加底座高度而填充大面积无信息土层、重复地层、空洞柱廊或虚构地下大厅；
- 地下尺寸未沿用统一比例尺 `S`，或B级推断使用了无依据的伪精确埋深。

合格结果必须满足：地下剖面是与地表建筑强相关的博物馆级结构或考古展示模型；已知事实优先，类型推断保守可解释，无法推断时诚实回退；所选 `2–5` 类元素共同说明建筑如何被支撑、使用或保存。

## 比例与编辑质量门禁

出现以下任意情况必须判定失败并针对比例问题重新生成：

- 可可靠核实的建筑或地标高度关系被颠倒，或主要对象的相对高度误差明显超过 10%；
- 同类建筑被无依据地拉成近似等高，或核心地标被无依据地夸大；
- 建筑、门、窗、楼层、树木、船只、车辆和人物之间出现明显不相容的尺度；
- 大型自然地形在未说明的情况下使用了独立或不一致的压缩比例；
- 核心山体压缩后低于 `0.20B`，或高于已确定的 `H_cap` 容差；
- 后续编辑没有沿用尺度表中的 `S`、`C` 或锁定高度；
- 用户只要求修改透视，但物理高度、占地、位置或未点名对象的画面尺寸明显改变；
- 单变量编辑后，未点名对象的画面包围盒宽高变化超过约 5%–8%。
- 地下空间、基础或埋深未沿用统一比例尺 `S`，或为了可见性被无记录地放大、加深。
- 底座厚度没有按建筑类型选定并记录 `T_base/B`，或成图厚度偏离目标值超过 `±0.02B`；
- 地下剖面在画面中明显过高：普通场景超过完整沙盘组高度的 `28%`，浅基础／坛基类超过 `22%`，且没有可靠深层结构依据。

合格结果必须满足：真实尺度有据可循；同类对象共用统一比例尺；必要的地形压缩使用单一、可追溯的 `C`；局部修改没有引入未授权的比例漂移。

## 3D 建模质量门禁

出现以下任意情况必须判定失败并重新生成：

- 沙盘底座的真实几何不是规则方形棱柱，或出现梯形、六边形、圆形、椭圆形、圆角、倒角、缺角、外扩、收腰及随地形变化的不规则外轮廓；
- 四条顶面外边不笔直、四角不是明确直角、侧面不垂直或底座厚度明显不一致；
- 地下剖面、观察窗或地层改变了立方体外轮廓，形成突出舱体、凹凸边界或附加台座；
- 大片地形看起来像照片贴在曲面上；
- 建筑看起来像航拍纹理而非独立模型；
- 森林呈现为连续毛毯状纹理；
- 超过 20% 的沙盘顶面为没有独立结构的平滑贴图区域；
- 建筑、树木或地形之间缺少清晰遮挡关系；
- 缺少微小接触阴影；
- 地形主体过高，使整个作品像山体雕塑而不是建筑沙盘；
- 模型表面缺少可辨识的厚度、边缘、台阶、墙体或层级；
- 第一眼更像“真实地点航拍”而不是“实体微缩模型摄影”。
- 地下剖面只是印在底座侧面的平面纹理，缺少真实空间、层厚、构件厚度和遮挡关系；
- 地下内容违反A级事实、B级类型推断或C级保守回退的证据规则，或出现宝藏、熔岩、幻想遗迹与超自然结构。

合格结果必须明显看到：`individual modeled objects + physical thickness + contact shadows + layered geometry`。

## 海报质量门禁

只在以下条件全部满足时接受结果：

- 输出画布为严格 `3:4` 竖版，`W:H = 3:4`；不得返回 1:1、4:3、9:16 或其他比例。
- 沙盘底座必须是完整规则的四边形立方体／方形棱柱：正方形顶面、四条笔直外边、四个 90° 角、垂直侧面和一致厚度；透视中可呈菱形，但不得变成梯形、多边形、圆角或不规则地形底座。
- 地下剖面高度必须克制：默认 `T_base = 0.12B–0.16B`、画面高度占完整沙盘组 `22%–28%`；天坛、祭坛、寺庙台基等浅基础场景使用 `T_base = 0.10B–0.13B`、画面高度占比 `18%–22%`。只有可靠深层结构才可提高，且地下不得压过地表主体。
- 场景身份和核心地标可辨识，但未直接复制原照片视角。
- 方形底座四条边、四个角、前侧与相邻侧面的地下剖面、其余模型框架侧边、菱形透视和高位等距视角全部清晰可见；沙盘元素未被裁切。
- 前侧及一个相邻侧面的地下剖面清晰可读，并与地表、建筑基础或地貌连续连接；已完成建筑类型判断并从六类构成中选择 `2–5` 类，地下内容符合A级事实、B级类型推断或C级保守回退，能够解释至少一项真实结构、工程、功能或历史关系。
- 沙盘顶面从边缘到边缘有丰富的独立建模对象与层级。
- 地形经过垂直压缩，没有单一山体或自然物统治整个沙盘。
- 建筑、地标、树木、交通工具与人物保持可信的真实高度关系；如使用地形压缩，压缩系数统一且核心地貌不过矮。
- 微小接触阴影与环境遮蔽足以呈现物理厚度，但整体光影仍然明亮柔和。
- 背景为干净、哑光、没有蓝色偏色的暖象牙米白色（约 `#F5F2EA`），沙盘周围保留足够安全边距。
- 右上方场景平面图位于 `72%–96%W`、`2.2%–22.2%H`，以无文字建筑总平面线稿呈现核心地标、道路、花园、水体、桥梁、围墙与地形边界；不含摄影点位、视线锥、导航信息、卫星纹理或地图 UI，且不与标题或沙盘重叠。
- 场景平面图与下方沙盘的平面关系一致；核心地标占地、主要路径、水体、花园分区、桥梁和边界均可相互对应，不得新增、删除或镜像关键结构。
- 包含地下剖面的完整沙盘组宽度为 `94%–96%W`、高度为 `47%–50%H`，左右边界约为 `2%–3%W` 与 `97%–98%W`，整体位于 `22%–71%H`；沙盘与底部信息表之间保留 `3%–5%H` 的干净间隔。
- 标题文字居中并使用深炭灰色；英文国家名称以小字位于 `8.8%–10.4%H`，英文地点名称以极粗窄体大字位于 `11.9%–18.4%H`。
- 地标最高点、平面图或沙盘阴影不得遮挡、穿过或降低标题字母可读性。
- 底部信息表位于 `6.7%–93.3%W`、`75%–95%H`，外框约宽 `86.6%W`、高 `20%H`；水平中线在 `85%H`，上排两等分，下排左半再等分，使用无填充、无圆角、无阴影的 `1 px` 深炭灰网格。
- 信息表完整显示 `Name`、`LOCATION`、`BUILT`、`HEIGHT`、`COORDINATES` 及对应值；标签和值使用单色等宽字体、左对齐、严格单行，不得乱码或溢出。
- 五项信息与国家名、主标题和沙盘身份完全一致；可核验字段准确，无法核验字段按规则使用 `N/A` 或 `Location unknown`，不复制参考图占位数据。
- 信息表下方保留约 `5%H` 的暖象牙留白，不添加页码、标志、脚注或装饰。
- 整体感受精致高级、具有收藏感，并明确是一件真实实体模型的摄影作品。
